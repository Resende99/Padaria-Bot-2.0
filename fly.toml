[app]
  # Nome do app no fly.io; será substituído/confirmado durante 'fly launch'
  app = "Padaria-Bot-2.0"
  kill_signal = "SIGINT"
  kill_timeout = 5
  processes = []

[build]
  image = ""

[env]
  PORT = "5000"

[experimental]
  allowed_public_ports = []
  auto_rollback = true
